# Shared Python dependencies for this monorepo
# Use from each workspace:
#   python -m pip install -r ../requirements.txt

# ONNX Inference
onnxruntime-gpu

# Shared / real robot navigation workspace
numpy>=1.21.0,<2.0.0
opencv-python>=4.5.0

# Real robot segmentation stack (PyTorch + models)
torch>=2.0.0
torchvision>=0.15.0
segmentation-models-pytorch==0.3.3
albumentations>=1.3.0
ultralytics==8.4.8
ultralytics-thop==2.0.18
